# API v2 Synthetic Fixtures

These canonical JSON fixtures exercise only the project-owned API v2 model:

- a local hello supporting versions 2 and 1;
- a remote hello supporting versions 3 and 2;
- a no-overlap hello supporting versions 4 and 3;
- one API v2 command-registration round trip.

They do not describe or fingerprint a built Avalon Companions assembly. Passing them proves serialization and negotiation behavior only.
